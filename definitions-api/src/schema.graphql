scalar File

type Category {
      _id: ID!
      name: String!
      categoryId: Int!
}

type Ingredient {
      _id: ID!
      name: String!
      categoryId: Int!
}

type Query {
      hello: String
      categories: [Category!]!
      ingredients: [Ingredient!]!
}

type Test {
    id: ID!
}